NODE_ENV=development
PORT=4000

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/dentflow?retryWrites=true&w=majority

# Frontend origin allowed to call this API (comma-separate multiple if needed later)
CORS_ORIGIN=http://localhost:3000

# Reserved for Milestone 1 (auth) - not used yet
# JWT_SECRET=
